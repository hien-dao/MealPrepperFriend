{
  "name": "mealprepperfriend",
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "mvn -f backend/pom.xml spring-boot:run",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
